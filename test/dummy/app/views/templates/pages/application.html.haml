- meta do
  - capture_haml do
    %meta{:name => "Description", :content => "Roxanne a simple CMS"}
    %meta{:name => "Keywords", :content => "Roxanne, Ruby, Ruby on Rails, Mercury Editor"}

= title "Roxanne - #{@page.title}"

%header
  Header
= @page.render_navigation 'navigation', :depth => 2

%article.main
  =raw @page.render_list('main')

%footer
  Footer
